version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  #install:
    #runtime-versions:
      # name: version
      # name: version
    #commands:
      # - command
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
      - chmod +x ./gradlew
      - ./gradlew assembledevDebug
  #post_build:
    #commands:
      # - command
      # - command
artifacts:
  files:
    - 'app/build/outputs/apk/dev/debug/app-dev-debug.apk'
    # - location
  #name: $(date +%Y-%m-%d)
  discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths
